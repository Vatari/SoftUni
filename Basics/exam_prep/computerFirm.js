function computerFirm(input) {
  let computerModels = Number(input[0]);

  let index = 1;
  let num = input[index] + "";

  let n1 = Number(num[0]);
  let n2 = Number(num[1]);
  let n3 = Number(num[2]);

  for (let a = num; a <= input.length; a++) {}
  console.log(computerModels.toFixed(2));
}
computerFirm(["3", "103", "103", "103"]);
